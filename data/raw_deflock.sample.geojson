      - name: Fetch latest Flock cameras
        run: |
          set -e
          mkdir -p data
          node fetch-alprs.js --out data/raw_deflock.geojson || node fetch-alprs.js > data/raw_deflock.geojson
          ls -lh data/raw_deflock.geojson || true

